{
	"buildCommand": "cd ../.. && pnpm install --frozen-lockfile && pnpm --filter admin build",
	"installCommand": "echo skip",
	"outputDirectory": "build/client",
	"rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
